meta {
  name: Edition (Unique)
  type: http
  seq: 2
}

get {
  url: {{base}}/editions/{{editionId}}
  body: none
  auth: bearer
}

headers {
  ~authorization: {{token}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  editionId: 967
}
