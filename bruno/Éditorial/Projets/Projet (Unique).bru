meta {
  name: Projet (Unique)
  type: http
  seq: 2
}

get {
  url: {{base}}/projets/{{projetId}}
  body: none
  auth: bearer
}

headers {
  ~authorization: {{token}}
}

auth:bearer {
  token: {{jwtToken}}
}

vars:pre-request {
  projetId: 1
}
