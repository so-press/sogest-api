meta {
  name: Réservations (Annuler)
  type: http
  seq: 6
}

delete {
  url: {{base}}/reservations/{{reservationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

vars:pre-request {
  reservationId: 52519
}

docs {
  Mise à la corbeille (`trash = 1`) : la réservation disparaît du planning mais
  reste en base, et l'annulation est tracée dans `historique`.
}
